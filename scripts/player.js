export default class Player {
    constructor(name, id, color) {
        this.name = name; // name of player
        this.id = id; // id of player (for marking spaces array)
        this.color = color; // color of player's checkers
    }
}